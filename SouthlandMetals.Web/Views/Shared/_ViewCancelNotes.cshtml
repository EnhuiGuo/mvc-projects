@model SouthlandMetals.Web.Models.NotesViewModel

<div class="modal fade" data-keyboard="false" data-backdrop="static" id="viewCancelNotesModal" tabindex="-1" role="dialog" aria-labelledby="basicModal" aria-hidden="true">
    <div class="modal-sm">
        <div class="modal-content">
            @Html.AntiForgeryToken()
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">Cancel Notes</h4>
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <div class="col-md-9 centered">
                        <div class="form-group">
                            @Html.LabelFor(m => m.CancelNotes)
                            @Html.TextAreaFor(m => m.CancelNotes, new { @class = "form-control", @style = "font-size: 13px;overflow-y: scroll;", cols = "50", rows = "10", @readonly = "readonly" })
                        </div>
                    </div>
                </div>
            </div>
            <br />
            <div class="modal-footer">
                <button id="closeBtn" class="btn btn-sm btn-default" type="button" data-dismiss="modal" title="Close">Close</button>
            </div>
        </div>
    </div>
</div>